services:
  - type: web
    name: spendzen-api
    rootDir: server
    env: node
    plan: free
    buildCommand: rm -rf node_modules && npm install --include=dev && npx prisma generate && npx prisma db push
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: file:./dev.db
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 5000
